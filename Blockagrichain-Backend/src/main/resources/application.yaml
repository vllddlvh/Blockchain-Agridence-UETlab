spring:
  application:
    name: traceability-service

  # Database Configuration
  datasource:
    url: jdbc:postgresql://localhost:5432/traceability_system
    username: postgres
    password: phuong4404
    driver-class-name: org.postgresql.Driver

  # JPA / Hibernate
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: buihaiphupng6@gmail.com
    password: mdfabuwjbybeuaal
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com

jwt:
  signerKey: "fAhsFQLtyhPPawzKmGDEU/IDeBpV7nGnIaQIWYZCzh8qGSb+ETsbFOoMfwfNsUgA"
  valid-duration: 3600 # 1 giờ (giây)
  refreshable-duration: 2592000 # 30 ngày (giây)